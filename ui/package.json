{
  "name": "agent-manager-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Optional read-only dashboard for agent-manager. Built separately; the core CLI has no runtime dependency on it.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
